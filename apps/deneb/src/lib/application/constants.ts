import { visual as metadata } from '../../../pbiviz.json';

export const APPLICATION_DESCRIPTION = metadata.description;

export const APPLICATION_VERSION = metadata.version;

export const APPLICATION_NAME = metadata.displayName;
