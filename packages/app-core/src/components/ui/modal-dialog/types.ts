/**
 * Modal dialog type (used for specific ops handling). `'mapping'` (the
 * remap-fields dialog) was removed along with its only caller — see
 * docs/plans/2026-09-15-001-refactor-editor-package-extraction-plan.md, U6.
 */
export type ModalDialogType = 'new' | 'export';
