{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["^build", "@deneb-viz/app-core#build"],
            "outputs": ["dist/**"]
        }
    }
}
