/**
 * This is not connected to the rest of the package and should not be included in any APIs, but is
 * used to generate the JSON schema for the usermeta template, which is a build artifact.
 */
export type { UsermetaTemplate } from '@deneb-viz/core-dependencies/src/definitions/template-usermeta-schema';
