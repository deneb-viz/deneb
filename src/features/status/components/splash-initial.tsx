/**
 * Temporarily return nothing until we can do this better. See notes on #395.
 */

export const SplashInitial = () => {
    return null;
    /*
        <StatusContainer>
            <Progress description={translate('Initial_Loading_Message')} />
        </StatusContainer>
    */
};
