import SettingsBase from './settings-base';

/**
 * Provides overrides to the General pane that gets created now that we're
 * using dynamic format strings.
 */
export default class GeneralSettings extends SettingsBase {}
